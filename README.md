# Example
this is my repo
<br> 
(br is command for next line)
so this is done by sharath 
clone command
ls is to list files in folder and cd is change directory cd .. is to come of the directory
git status is to view status of commit
first we need to add file using git add filename
next is commit it records the change
git commit -m"some msg"
git add . is used to add all at once


push command is used to upload local(my computer) to remote repo(github repo)
git push origin main.   #(command used)
git init  #used to create new repo in local system
once we create new repo in local pc n we neeed to push it to repo in github so we create a new repo so we use command git remote add origin link
git remote -v #to verify remote 
git branch #to check branch
git branch -M main # to rename branch
git push origin name #final push



GIT BRANCHES
 branches are diff commits for one project like difff teams working on same project and we can merge them afterwards 
branch commands
git branch #to check branch
git branch -M newname #to rename branch
git checkout branch name # to navigate or switch branches
git checkout -b new branch name #(to create new branch)
git branch -d branch name # to delete branch
